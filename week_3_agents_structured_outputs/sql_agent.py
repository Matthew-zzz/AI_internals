"""
Файл: sql_agent.py
Неделя 3: Автономный SQL-агент с циклом самоисправления (Self-Correction Loop).

ЧТО ЗДЕСЬ ДОЛЖНО БЫТЬ РЕАЛИЗОВАНО:
1. Инициализация базы данных SQLite / PostgreSQL (например, встроенная БД с продажами/пользователями).

2. Шаги работы Агента (ReAct Pattern):
   - ШАГ 1 (Schema Inspection): Агент запрашивает имена таблиц и схемы колонок через PRAGMA table_info().
   - ШАГ 2 (Query Generation): Агент генерирует SQL-запрос под вопрос пользователя, обернутый в Pydantic-схему.
   - ШАГ 3 (Execution): Агент исполняет SQL-запрос на БД.
   - ШАГ 4 (Self-Correction Loop):
     * Если БД возвращает синтаксическую ошибку (например, `no such column`), агент перехватывает exception.
     * Агент отправляет в LLM историю: "Ты сгенерировал SQL X, но БД выдала ошибку Y. Исправь запрос."
     * Повторяет выполнение (до 3 попыток).
   - ШАГ 5 (Final Answer): Формирование отчета на естественном языке с графиком.
"""
