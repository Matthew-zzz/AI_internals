"""
Файл: eval_rag_metrics.py
Неделя 2: Расчёт метрик качества поиска (Hit Rate@K и MRR).

ЧТО ЗДЕСЬ ДОЛЖНО БЫТЬ РЕАЛИЗОВАНО:
1. Функция calculate_hit_rate(test_dataset, k=5):
   - Доля запросов, для которых релевантный документ попал в Top-K результатов поиска.
   - Hit Rate = (Число успешных попаданий) / (Общее число тестовых запросов).

2. Функция calculate_mrr(test_dataset):
   - MRR (Mean Reciprocal Rank) — учитывает ПОЗИЦИЮ правильного документа в выдаче.
   - Если правильный документ на 1 месте -> Rank = 1/1 = 1.0.
   - Если на 2 месте -> Rank = 1/2 = 0.5.
   - Если на 3 месте -> Rank = 1/3 = 0.33.
   - MRR = Среднее арифметическое по всем запросам.

3. Профайлинг:
   - Запуск оценки качества для 3 конфигураций RAG:
     а) Только Dense Search
     б) Dense + BM25 (Hybrid)
     в) Hybrid + Cross-Encoder Reranking (Ожидается наивысший MRR).
"""
